//Utilizado en ProfilePage para mostrar detalles en la seccion de ProfileDetailSection
//Utilizado en el modal, el head se utiliza para el label y el body para el texto que contiene inicialmente cada textbox

//Siendo que es muy simple la interfaz, probablemente se podra dar un uso mas general y renombrar a algo mas generico

export interface IProfileDetail
{
  head: string;
  body: string;
}